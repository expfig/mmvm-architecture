/**
 * IMPORTS
 */

import {AppOpenRoutesStacks} from './routes-stack';
// import { ButtonWs, TextCustom } from "react-native-components-ws";

const AppRoutes = () => {
  return <AppOpenRoutesStacks />;
};

/**
 * EXPORT
 */
export {AppRoutes};
